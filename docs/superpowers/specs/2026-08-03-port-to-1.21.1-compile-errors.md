# 1.21.1 port — compile error capture (sub-project 1 output)

Captured from `JAVA_HOME=~/.local/jdks/temurin-21 ./gradlew clean build` (rerun with `--continue`
to confirm no additional errors were hidden) on branch `mc1.21.1-port`, commit `2510bfc` ("Bump
Java compile target to 21 for the 1.21.1 port" — Task 3's commit).

## Summary

- Total distinct errors: 1
- Failure stage: Gradle project configuration / dependency resolution — the build fails before
  `compileJava` (or any task) ever starts. Rerunning with `--continue` reproduced the exact same
  single error, confirming this isn't a truncated list: `--continue` only lets independent
  *tasks* keep running after one fails, but a project-configuration failure happens before Gradle
  has any tasks to execute at all, so there is nothing else for it to surface here.
- Pattern: single root cause — `gradle.properties`' pinned `apoli_version` value
  (`2.12.0-pre.3+mc.1.21.1`) does not correspond to a real published artifact on JitPack. The
  request `GET https://jitpack.io/com/github/apace100/apoli/2.12.0-pre.3+mc.1.21.1/apoli-2.12.0-pre.3+mc.1.21.1.pom`
  returns HTTP 400 (Bad Request), which JitPack returns when it cannot build/serve that exact
  version string (e.g. the git tag doesn't exist, or the version string doesn't match tag naming
  the jitpack build picks up). No Java source in this project was ever reached by the compiler —
  none of the 17+ mixins or other Java files have been validated against 1.21.1 APIs yet. This
  entirely scopes sub-project 2's first job: find the correct/real published coordinate (tag) for
  an Apoli build targeting Minecraft 1.21.1 on JitPack (or an alternative distribution channel),
  fix the pin, and only then will the build reach `compileJava` and reveal any real source-level
  incompatibilities.

## Full error list

### `gradle.properties:32` (`apoli_version`, consumed by `build.gradle:90`)

```
FAILURE: Build failed with an exception.

* What went wrong:
A problem occurred configuring root project 'monster_origins'.
> Could not resolve all files for configuration ':modCompileOnly'.
   > Could not resolve com.github.apace100:apoli:2.12.0-pre.3+mc.1.21.1.
     Required by:
         root project 'monster_origins'
      > Could not resolve com.github.apace100:apoli:2.12.0-pre.3+mc.1.21.1.
         > Could not get resource 'https://jitpack.io/com/github/apace100/apoli/2.12.0-pre.3+mc.1.21.1/apoli-2.12.0-pre.3+mc.1.21.1.pom'.
            > Could not GET 'https://jitpack.io/com/github/apace100/apoli/2.12.0-pre.3+mc.1.21.1/apoli-2.12.0-pre.3+mc.1.21.1.pom'. Received status code 400 from server: Bad Request

BUILD FAILED in 10s
```
